"""SQLAlchemy model namespace.

The current Express backend does not persist runtime sheet data into PostgreSQL,
and `packages/db/src/schema/index.ts` is intentionally empty. To preserve
functional parity and schema behavior, no tables are declared here yet.
"""

